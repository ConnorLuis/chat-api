# 2026-01-11（Day11）日志｜Streaming Demo 页面（/demo）

## 今日目标
把后端从“只有 API”升级成“可交互 Demo”，能在浏览器中实时看到 SSE 流式输出：

- 新增 `/demo` 页面：支持选择 provider（mock/ollama），输入 prompt，一键开始流式聊天
- 页面展示：
  - meta：trace_id/provider/model
  - token：逐步拼接输出
  - usage：latency_ms、token 统计（如有）
  - error：结构化错误信息（可定位 trace_id）
- 新增最小契约测试：`/demo` 页面可访问，且包含 SSE 关键字

---

## 今日完成 ✅

### 1) 新增 Demo 路由
文件：`src/app/api/routes_demo.py`

- `GET /demo` 返回最小 HTML 页面（内联 JS）
- 使用 `fetch(POST /chat/stream)` + 读取 `ReadableStream` 的方式解析 SSE
  - 用 `\n\n` 分割事件块（event/data）
  - 根据 event 类型更新 UI：meta/token/usage/error/done

---

### 2) 新增测试（demo 页面契约）
文件：`tests/test_demo_page.py`

验证点：
- `GET /demo` 返回 200
- `content-type` 包含 `text/html`
- 页面内容包含 `/chat/stream`，以及 `event:/token/meta/usage/done` 等关键字（确保 SSE 解析逻辑存在）

---

## 今日验收（DoD）
- [x] Demo 页面在 mock/ollama 两种 provider 下都可工作（能看到 meta/输出/结束）
- [x] `pytest -q` 全绿
- [x] 代码已 commit & push

---

## 明日计划（Day12）
- Demo 增加 Stop/Abort（AbortController）
- 可选：为 usage 增强 input/output token 统计
