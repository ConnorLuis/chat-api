## 今日完成（Day8）
- 统一错误模型：新增 `build_error()`，让 `/chat` 与 SSE `event:error` 共用同一结构（trace_id/provider/model/latency_ms/error）
- `/chat` 错误工程化：下游失败返回 502，并在 `detail` 中携带结构化错误
- SSE 错误契约：`/chat/stream` 建立连接为 200，业务失败通过 `event:error` 传递结构化 JSON
- 日志工程化：引入 logging 初始化（main 中 setup），路由与中间件统一使用 logger 输出关键字段
- 契约测试：新增 `/chat` 错误契约测试 + stream error 契约测试，并修复 SSE 解析误取 meta data 的问题
- 强化一致性：SSE `meta` 增加 `model` 字段；统一 model 兜底为 `"unknown"`

## 验收
- `pytest -q`：8 passed
- `/chat` 下游失败：返回 502 + `detail` 结构化 JSON
- `/chat/stream` 下游失败：200 + `event:error` 结构化 JSON

## 今日卡点
- 环境变量前缀只影响当前进程；要影响服务端必须在启动 uvicorn 的 shell 设置 env 或重启服务
- SSE 测试解析要从 `event:error` 之后取 `data:`，避免误读 `meta` 的 data

## 明日计划（Day9）
- README 补充错误示例与事件说明（meta/token/usage/done/error）
- 进一步统一返回结构：可选增加 `event: done` 在 error 后的收尾策略
